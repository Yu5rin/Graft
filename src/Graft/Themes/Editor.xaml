<!--
  4.1: AvalonEditのテーマ適用。既定配色を一切残さず、9.3のカラートークンへ完全に
  追従させる。App.xamlのMergedDictionariesから読み込まれ、アプリ全体で1つだけロードされる
  （Views/EditorPane.xamlは自分自身では二重にマージしない。UserControlは親Windowの
  リソースをStaticResourceで参照できない問題＝附録ブリーフの既知の落とし穴2は、
  App.Resourcesに積まれたリソースには影響しない。StaticResource/DynamicResourceは
  いずれも論理ツリーをApplication.Resourcesまで遡って解決するため）。

  TextArea.SelectionBrush・TextView.CurrentLineBackground等はTextEditorの
  スタイルSetterからは設定できないDP（TextEditor自身ではなく子要素が持つプロパティ）
  のため、それらはViews/EditorPane.xaml.csからSetResourceReferenceで設定する。
  このファイルはStyle/ControlTemplateとして表現できる範囲を担当する。
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:ae="http://icsharpcode.net/sharpdevelop/avalonedit">

    <!-- ========== AvalonEdit TextEditor 本体 ========== -->
    <Style TargetType="ae:TextEditor">
        <Setter Property="Background" Value="{DynamicResource BgBase}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
        <Setter Property="FontFamily" Value="{DynamicResource CodeFontFamily}" />
        <Setter Property="LineNumbersForeground" Value="{DynamicResource TextDisabled}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="4" />
    </Style>

    <!-- ========== エディタタブの見出し部 ========== -->
    <Style x:Key="EditorTabStripStyle" TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource BgSurface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderSubtle}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" IsItemsHost="True" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- タブ1件のコンテナ。選択中は下線と背景で示す（色だけに依存しない: 太字化も併用）。 -->
    <Style x:Key="EditorTabItemStyle" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="0" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="AutomationProperties.Name" Value="{Binding ToolTipText}" />
        <Setter Property="ToolTip" Value="{Binding ToolTipText}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Root" Background="Transparent"
                            BorderBrush="{DynamicResource Accent}" BorderThickness="0,0,0,2">
                        <ContentPresenter Margin="10,6" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="False">
                            <Setter TargetName="Root" Property="BorderBrush" Value="Transparent" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource BgBase}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource BgHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- タブを閉じるボタン（9.4のxアイコンを使用）。 -->
    <Style x:Key="EditorTabCloseButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="18" />
        <Setter Property="Height" Value="18" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" Background="{TemplateBinding Background}" CornerRadius="3">
                        <Path Data="{StaticResource IconXGeometry}"
                              Width="12" Height="12" Stretch="Uniform"
                              Fill="{x:Null}"
                              Stroke="{DynamicResource TextSecondary}"
                              StrokeThickness="{DynamicResource IconStrokeThickness}"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                              HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{DynamicResource BgHover}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
