# 確認手順: 前面化の修正・最小化設定・履歴の欠番

前回の Windows 実機確認で NG だった 2 件の修正と、新しく追加した設定の確認です。
所要時間は 20 分程度を想定しています。

対象の変更:

| コミット | 内容 | 実機確認 |
|---|---|---|
| `ada507a` | 前面化の経路をウィンドウハンドル直接指定へ変更（**2-2 の修正**） | 必要 |
| `ada507a` | 前面化オフのとき自動解析による前面化をやめた（**2-5 の修正**） | 必要 |
| `ada507a` | 最小化でトレイへ格納するかを設定に（既定オフ） | 必要 |
| `8613d1f` `e15b6c6` `db25868` `22b2768` | テストの安定化のみ | 不要 |

---

## 1. ビルド

```powershell
cd C:\Users\YUGO\Graft
git pull origin claude/spec-implementation-7fhplo
git log --oneline -1
```

1行目が `db25868` で始まっていれば最新です。

```powershell
cd C:\Users\YUGO\Graft
Remove-Item -Recurse -Force publish\Graft -ErrorAction SilentlyContinue
dotnet publish src\Graft\Graft.csproj -c Release -r win-x64 --self-contained true -o publish\Graft
```

> **注意**: 発行先には `settings.json` `projects.json` `back/` `logs/` も置かれます。
> `Remove-Item` でそれごと消えるため、履歴を残したい場合は先に退避してください。

`publish\Graft\Graft.exe` を起動してから、以下へ進んでください。

---

## 2. 前面化（今回の本命）

### 2-1. 背面にある Graft が前面に出る　★最重要

前回 **NG** だった項目です。

- [ ] Graft を起動したまま、**メモ帳など別のアプリを前面**にする（Graft は背後）。
- [ ] メモ帳に次を貼り付け、**全選択してコピー**する。

```
<<<< PATCH
summary: 前面化の確認1
type: feat
>>>>

<<<< FILE: note.txt
<<<<<<< SEARCH
バージョン: 初期
=======
バージョン: 1
>>>>>>> REPLACE
```

**期待**: コピーした瞬間に Graft が前面に出てくる。

**NG のとき報告してほしいこと**:
- タスクバーの Graft アイコンが**点滅したか**
- 次のログ（末尾20行）

```powershell
Get-Content -Encoding UTF8 (Get-ChildItem C:\Users\YUGO\Graft\publish\Graft\logs\*.log | Sort-Object LastWriteTime | Select-Object -Last 1) -Tail 20
```

### 2-2. オフ＋「トレイ通知のみ」でバルーンが出る　★最重要

前回 **NG**（前面に出てしまい、バルーンが出なかった）だった項目です。

- [ ] 設定 →「表示・コンテキスト」タブ →「検知したら前面に表示する」の**チェックを外す**。
- [ ] 同じタブの「反応時の挙動」を **「トレイ通知のみ」** にする。
- [ ] 設定を閉じ、Graft を背面に置いて、**別の内容**のパッチをコピーする（`バージョン: 1` → `バージョン: 2` など。同じ内容だと反応しません）。

**期待**: **前面に出ず**、タスクトレイに**バルーン通知が出る**。

**NG のとき報告してほしいこと**: 前面に出てしまったか／通知が出なかったか（両方あり得ます）。

### 2-3. 最小化から復帰する（回帰していないか）

前回 OK だった項目です。壊れていないことの確認。

- [ ] 2-2 で外したチェックを**元に戻す**（「検知したら前面に表示する」をオン）。
- [ ] Graft を**最小化**し、また別の内容のパッチをコピーする。

**期待**: 最小化から復帰して前面に出る。

### 2-4. 既に前面なら何も起きない

- [ ] Graft を**前面にしたまま**、また別の内容のパッチをコピーする。

**期待**: ウィンドウの位置・大きさが変わらず、ちらつきもない。

### 2-5. 通常のテキストには反応しない

- [ ] 「これはただの文章です」など、パッチ形式でないテキストをコピーする。

**期待**: 前面化も通知も起きない。

---

## 3. 最小化の挙動（新設定）

### 3-1. 既定ではタスクバーに残る　★変更点

- [ ] 設定 →「一般」タブに **「最小化でタスクトレイへ格納する」** があり、**チェックが外れている**ことを確認する。
- [ ] マウスを乗せて説明が日本語で出ることを確認する。
- [ ] 設定を閉じ、Graft を**最小化**する。

**期待**: **タスクバーに Graft が残っている**（従来はここで消えていました）。タスクバーのアイコンをクリックすると元に戻る。

### 3-2. オンにするとトレイへ入る（再起動なしで切り替わる）

- [ ] 設定 →「一般」→「最小化でタスクトレイへ格納する」を**オンにする**。
- [ ] 設定を閉じ、**アプリを再起動せずに**最小化する。

**期待**: タスクバーから消え、**タスクトレイのアイコンからのみ**復帰できる。

- [ ] トレイアイコンをクリックして復帰させ、設定を**オフに戻す**。
- [ ] **再起動せずに**もう一度最小化する。

**期待**: タスクバーに残る（3-1 と同じ状態に戻る）。

**NG のとき報告してほしいこと**: 再起動しないと切り替わらなかったか、どちらの方向で切り替わらなかったか。

---

## 4. 履歴の欠番（前回できなかった項目）

サンプルを生成します。

```powershell
cd C:\Users\YUGO\Graft
powershell -ExecutionPolicy Bypass -File tools\New-RevisionTestSample.ps1
```

デスクトップに `Graft履歴検証サンプル` ができます。**エラーが出たらその文面をそのまま報告してください。**

- [ ] Graft で `Graft履歴検証サンプル\project` をプロジェクトとして登録する。
- [ ] `patches\01_r1.txt` 〜 `04_r4.txt` の中身を順にコピーして貼り付け、**4回「適用」**する。履歴に **r1〜r4** が並ぶことを確認する。
- [ ] 履歴で **r2 を右クリック →「ここまで戻す」**。**r5** として記録されることを確認する。
- [ ] **もう一度 r2 を右クリック →「ここまで戻す」**。

**期待**: 「変更はありませんでした。リビジョンは記録していません」という趣旨のメッセージが出て、**履歴に新しい行は増えない**。

- [ ] その状態で次のパッチを貼り付けて適用する。

```
<<<< PATCH
summary: 欠番確認用
type: feat
>>>>

<<<< FILE: note.txt
<<<<<<< SEARCH
バージョン: 2
=======
バージョン: 9
>>>>>>> REPLACE
```

**期待**: 新しいリビジョンが **r6** になる（**r7 に飛ばない**）。

**NG のとき報告してほしいこと**: 実際に付いた番号と、次の出力。

```powershell
Get-Content -Encoding UTF8 C:\Users\YUGO\Graft\publish\Graft\projects.json
```

---

## 5. 通しの動作確認（壊れていないか）

テストの安定化で多くのファイルを触ったため、基本動作の確認もお願いします。

- [ ] 起動して、前回のウィンドウ位置・サイズ・開いていたタブが復元される。
- [ ] プロジェクトを切り替えられる。
- [ ] ファイルをエディタで開いて編集し、Ctrl+S で保存できる。
- [ ] エクスプローラでファイルを削除し、**Ctrl+Z で元に戻る**。
- [ ] ×ボタンで終了でき、`logs\<日付>.log` に終了処理の記録が残る。

---

## 6. 報告用メモ（コピーして使ってください）

```
git log の1行目:              （そのまま貼る）
dotnet publish:               成功 / 失敗（　　　　）

2-1 背面から前面化:           OK / NG（　　　　）★ / 点滅のみ
2-2 オフ＋トレイ通知:         OK / NG（　　　　）★
2-3 最小化から復帰:           OK / NG（　　　　）
2-4 前面時は無反応:           OK / NG（　　　　）
2-5 通常テキストに無反応:     OK / NG（　　　　）

3-1 既定でタスクバーに残る:   OK / NG（　　　　）★
3-2 設定の即時切り替え:       OK / NG（　　　　）

4  番号が飛ばない:            適用後の番号 = r　　（期待: r6）

5  起動・レイアウト復元:      OK / NG（　　　　）
5  プロジェクト切替:          OK / NG（　　　　）
5  編集と保存:                OK / NG（　　　　）
5  削除の Ctrl+Z:             OK / NG（　　　　）
5  終了とログ:                OK / NG（　　　　）

気づいたこと:

```

---

## 7. 後片付け

- `Graft履歴検証サンプル`（デスクトップ）は削除して構いません。
- 設定を触った場合は、設定画面の「既定に戻す」で戻せます（`projects.json` は変更されません）。
