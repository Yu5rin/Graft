# 変更履歴

## 1.0.15

### 修正（セキュリティ）
- **AIの回答に紛れ込ませたパッチで、プロジェクトの `.git` フォルダの中身（`.git/config`・`.git/hooks/pre-commit` など）を書き換えられてしまう問題を修正した。** セキュリティ点検で見つかったもので、実機での被害報告は無いが、**放置すると利用者の環境で任意のコマンドが実行されうる**性質のため最優先で塞いだ
  - 【何が起きうるか】 gitは対象リポジトリの `.git/config` を読み、`core.fsmonitor` などの設定値を**コマンドとして実行する**。Graftは「Gitの変更マーカー」（`editor.gitGutter`）が**既定でオン**で、プロジェクトルートを作業ディレクトリとして `git` を起動するため、`.git/config` を書き換えられた時点で、利用者が何も操作しなくてもこの経路が踏まれる。`.git/hooks/pre-commit` などのフックも、`git commit` のたびに自動実行される（「適用後に自動コミット」がオンなら適用直後に、オフでも利用者自身の次のコミットで走る）
  - 【実測】 `<<<< FILE: .git/hooks/pre-commit MODE=FULL` を含むパッチが、解析（適用可）から適用まで一度も止まらずに通り、フックの中身が実際に書き込まれることを確認した。`.git/config`・`.git/hooks/post-checkout`・`.hg`・`.svn` も同様だった
  - 【修正】 **バージョン管理の内部フォルダ（`.git`・`.hg`・`.svn`）の配下は、経路を問わず常に拒否する**ようにした（新しいエラーコード **E211**）。パッチ適用だけでなく、エクスプローラからの新規作成・フォルダ作成・ファイルの取り込みでも同じ規則が効く。大文字小文字は区別しない（Windowsでは `.GIT` も同じ場所を指すため）。判定はセグメント全体の一致で行うので、**`.gitignore` や `.github` フォルダは対象外**で、これまでどおり編集できる。サブモジュールなど途中の階層にある `.git` も同じ理由で拒否する
- **拡張子の無いファイル名がすべて無条件に書き込み可能だった問題を修正した。** 拡張子ホワイトリスト（設定の「許可する拡張子」）は `.exe` や `.bat` の書き込みを防ぐための仕組みだが、`Dockerfile` や `LICENSE` を通すために入れた対応が、実際には「**拡張子が無ければ検査そのものを飛ばす**」という作りになっていた
  - 【実測】 `Makefile`・`script`・`bin/entrypoint`・`pre-commit`・`CON`・`NUL`・`COM1` がすべて素通りしていた。拡張子が無いことは「安全である根拠」にはならない（実行権限を付ければ動く）
  - 【修正】 拡張子が無い場合は、**名前そのものの許可リスト**で判定するようにした。既定で許可するのは、利用者が明示的にビルドや実行のコマンドを叩いたときだけ読まれる種類のもの（`Dockerfile`・`Containerfile`・`Makefile`・`GNUmakefile`・`Rakefile`・`Gemfile`・`Procfile`・`Jenkinsfile`・`Vagrantfile`・`Brewfile`・`Justfile`）と、説明・法務系のテキスト（`LICENSE`・`LICENCE`・`COPYING`・`NOTICE`・`README`・`CHANGELOG`・`HISTORY`・`AUTHORS`・`CONTRIBUTORS`・`CONTRIBUTING`・`TODO`・`VERSION`・`CODEOWNERS`）。大文字小文字は区別しない
  - **Windowsの予約デバイス名（`CON`・`NUL`・`COM1` など）も、この許可リスト方式の結果として自動的に拒否されるようになった**（個別の対応は入れていない）
  - 【利用者への影響】 エクスプローラの「新規ファイル」で、拡張子の無い**任意の**名前を作ることはできなくなった（上記の決まった名前は従来どおり作れる）。入力欄の案内文も実際の動作に合わせて直してある。**「ファイルを追加」とドラッグ＆ドロップによる取り込みは対象外**で、拡張子の無いファイルも画像等もこれまでどおり取り込める（取り込みは利用者が明示的に選んだ既存ファイルをコピーするだけの操作のため）
- **`evil.exe:payload.txt` のような、Windowsの代替データストリーム記法を使うと拡張子ホワイトリストを回避できてしまう問題を修正した。** `.NET` の拡張子判定（`Path.GetExtension`）はこの書き方に対して `.txt` を返すため、**実体は `.exe` なのに許可された拡張子として通過**していた（実測で確認）
  - 【修正】 パスの構成要素に `:` を含む場合は、パスを組み立てる前に拒否するようにした（新しいエラーコード **E212**）。Windowsではファイル名に `:` を使えないため、正常な用途を壊さない
  - 【検証の限界（正直な報告）】 Windows実機でこの書き方が実際にどう扱われるか（本当にデータストリームとして書き込まれるか）は**未確認**である。開発環境がLinuxのため確認できないが、「拡張子の切り出しがホワイトリストを回避する」こと自体はLinux上でも実測済みで、塞ぐことに害が無いため実機確認を待たずに塞いだ
- **`.gitignore` を作成・編集できなかった不整合を修正した。** `Path.GetExtension(".gitignore")` は `".gitignore"`（名前全体）を返すため、拡張子ホワイトリストから必ず外れ、`Makefile` は作れるのに `.gitignore` は作れない、という状態になっていた（実測でE202）
  - 【修正】 「先頭がドットで、それ以外にドットを含まない」名前は実質的に拡張子を持たない名前とみなし、上記の名前の許可リストで判定するようにした。既定で許可するのは `.gitignore`・`.gitattributes`・`.gitmodules`・`.gitkeep`・`.editorconfig`・`.dockerignore`・`.npmignore`・`.npmrc`・`.prettierrc`・`.prettierignore`・`.eslintrc`・`.eslintignore`・`.env`
  - **`.gitignore` は `.git` フォルダとはまったくの別物**であり、上の `.git` 配下の拒否とは混同していない（`.gitignore` はプロジェクト直下の普通のファイルとして編集できる）
  - シェルやdirenvが**起動時に自動で読み込む**種類の設定ファイル（`.bashrc`・`.profile`・`.zshrc`・`.envrc`）は、フックと同じ「勝手に走る」性質を持つため、あえて許可リストに入れていない
  - なお `.hidden.exe` や `.env.sh` のように2つ目のドットがある名前は、従来どおり拡張子（`.exe`・`.sh`）で判定される

#### 検証（書き込み先検査）
- 上記4件それぞれについて、**修正前は素通りしていたことを実測で確認したうえで**、修正後に拒否されることを自動テストで固定した（`tests/Graft.Tests/PathGuardWriteGuardTests.cs`）。報告された攻撃経路そのもの（`.git/hooks/pre-commit` を書くパッチが解析→適用まで通る）も、適用エンジンを通した端から端までのテストとして残している
- 正常な用途が壊れていないことも併せて固定した（`Dockerfile`・`Makefile`・`LICENSE`・`.gitignore`・`.editorconfig`・通常の `.cs` `.md` `.json` などが従来どおり通ること、取り込み・フォルダ作成の経路が影響を受けないこと）
- Xvfb上で実際にアプリを起動し、エクスプローラでの `.gitignore`・フォルダ・通常ファイルの新規作成、通常のパッチの解析と適用（r1として記録）、および `.git/hooks/pre-commit` を書くパッチが「0件適用可」となりE211の説明が表示されることを目視で確認した
### 修正（データ整合性）
- **プロジェクト一覧（`projects.json`）の読み書きに排他が無く、同時に走った操作どうしが互いの結果を消してしまう不具合を修正した。** 点検で実測した壊れ方は次のとおり
  - `ProjectStore.ConsumeNextRevisionAsync`（適用のたびにリビジョン番号を1つ払い出す処理）を20並行で呼ぶと、**20件すべてが同じ `1` を返し**、`nextRevision` は21ではなく**2**にしかならなかった
  - `ProjectStore.RegisterAsync`（フォルダの登録）を別々のフォルダで10並行に呼ぶと、**10件中2件しか `projects.json` に残らなかった**
  - 【原因】 更新系のメソッドがいずれも「読み込む → 加工する → 保存する」という読み書き分離の形をしており、その間に排他が一切無かった。そのため後から保存した側が「自分が読んだ時点の一覧」で全体を上書きしてしまう
  - 【利用者から見た実害】 リビジョン番号が重複すると、複数の適用が `back/<プロジェクトId>/` の**同じ番号のバックアップフォルダを同時に読み書き**することになり、履歴と実体の対応が壊れて「ここまで戻す」で戻せない世代ができる。登録が消えるほうは「登録したはずのフォルダがプロジェクト一覧に無い」という形で現れる
  - 【修正】 `projects.json` 1ファイルにつき1本の排他ゲート（`SemaphoreSlim`）を設け、「読み込み〜保存」を1つの臨界区間にまとめた。対象は `ConsumeNextRevisionAsync` / `RegisterAsync` / `UpdateAsync` / `RemoveAsync` / `RelocateAsync` / `MarkAppliedAsync` / `ReleaseRevisionAsync` と、公開APIの `LoadAsync` / `SaveAsync`。待ち受けは `await` で行うためUIスレッドは塞がらない
  - 【インスタンスではなくファイル単位にした理由】 `ProjectStore` はアプリ内で1個ではなく、`StartupCoordinator`・`SettingsViewModel`・`OnboardingWindow` がそれぞれ生成している。インスタンスごとに持つ排他では、別インスタンス同士が同じ `projects.json` を同時に書く経路を塞げないため、対象ファイルのパスをキーにしたゲートを共有する
  - 【デッドロックへの備え】 `LoadAsync` は破損データの救済で内部から保存し直す経路を持つ。`SemaphoreSlim` は再入できないため、臨界区間の中からは必ずゲートを取らない非公開版（`LoadCoreAsync` / `SaveCoreAsync`）を呼ぶ形へ分けた。登録・更新・削除・番号消費を混ぜて同時に走らせても時間内に全部完了することをテストで固定している
  - 【どこまで守るか】 守るのは**同一プロセス内**の並行実行だけで、プロセスをまたぐ排他は対象外とした。多重起動は起動時の単一インスタンス確保が別途防いでいるうえ、ファイルロックを持ち込むと異常終了時にロックが残って起動不能になる副作用のほうが実害が大きいため
- **キーボード操作からコマンドを多重に起動できてしまう不具合を修正した。** `AsyncRelayCommand.CanExecute` は実行中に false を返すのに、`Execute` は実行中かどうかも `CanExecute` も見ずに実行していた
  - ボタン経由はAvaloniaが押下前に `CanExecute` を見るため守られていたが、`Ctrl+Enter`（適用）・`Ctrl+Alt+Z`（取り消し）・`Ctrl+Shift+V`（貼り付けて解析）などのキーボード経路と、グローバルホットキーからの貼り付けは `Execute` を直接呼んでいるため素通しだった。実測では `Execute` を3回連続で呼ぶと、`CanExecute` が false を返す一方で処理は3回とも走った
  - 上の `projects.json` の排他が無い問題と組み合わさると実害になる。適用を3連打したところ、**確認ダイアログが3回出て適用処理が3本同時に走り**、それでも `nextRevision` は2のまま・記録されたリビジョンは `r1` の1件だけ・`back/` のフォルダも1つだけ、という状態を実測した（＝3つの適用が同じ `r1` のバックアップフォルダを奪い合っていた）
  - 【修正】 呼び出し側48箇所を1つずつ直すのではなく、`Execute` の先頭で `CanExecute` を見る1箇所の修正で全経路を塞いだ。同期版の `RelayCommand` / `RelayCommand<T>` にも同じ形のガードを置いている（同期版は呼び出しが戻るまでに完了するため多重には走らないが、「実行不可の状態で直呼びが素通しになる」点は同じであるため）
  - 「1回目が終わってから2回目」という通常の連続操作は従来どおり通る。押せないボタンと同じ条件で弾くだけであり、テストでも逐次操作が壊れていないことを固定した
  - 【正直な但し書き】 実機の適用・取り消しは直後にモーダルダイアログが出るため、2回目の `Ctrl+Enter` がダイアログ表示前のわずかな隙間に入る必要があり、**Windows実機での再現は確認できていない**（ヘッドレスのViewModelレベルでのみ再現）。ただし `projects.json` 側の被害が実際に出る経路であるため、構造として塞いだ
### 修正（安定性）
- **エディタ内検索（Ctrl+F / Ctrl+H）で「正規表現」をONにし、`(a+)+$` のような破滅的バックトラックを起こすパターンを入力すると、アプリがプロセスごと終了し未保存の編集内容がすべて失われる不具合を修正した。**
  - 【原因】 検索の正規表現は暴走防止のため**2秒のタイムアウト付き**で作っている（`SearchPatternBuilder`）。ところがこのタイムアウト例外（`RegexMatchTimeoutException`）は**正規表現を作るときではなく照合するとき**に飛ぶため、`SearchPatternBuilder` 側の `try` では決して捕まらない。そして照合を行う `SearchOverlayViewModel.CollectMatches` は例外を一切捕まえていなかった。抜けた例外はデバウンスのタイマー（`DispatcherTimer.Tick`）やキー入力ハンドラを素通りして `Dispatcher.UIThread.UnhandledException` へ届くが、`App.OnDispatcherUnhandledException` は例外の発生元がAvaloniaEditのときしか握らない設計（`AvaloniaEditExceptionGuard`）のため素通しし、`AppDomain.UnhandledException` まで抜けてプロセスが終了していた
  - 【特に厄介だった点】 発火はデバウンス（150ms）経由でも起こるため、**検索語を打っている途中の中間状態**（例えば `(a+)+` まで打った時点）でも落ちうる。「確定していないのだから大丈夫」という直感が通用しない
  - 【修正】 全文走査（`CollectMatches`）と「すべて置換」後の再走査で `RegexMatchTimeoutException` を捕まえ、**不正な正規表現を入れたときとまったく同じエラー表示**（検索欄のエラー色と理由の表示）に落とすようにした。文言は「正規表現の処理に時間がかかりすぎたため中断しました。パターンを簡単にしてください。」で、原因だけでなく次の一手まで書いている。打ち切られた時点までに拾えていたヒットは「文書の先頭側だけを見た結果」でしかなく、件数表示（「3 / 12」）もハイライトも実態と食い違うため、残さず破棄する
- **プロジェクト全体検索（Ctrl+Shift+F）で同じパターンを入力したときに、「想定外のエラー」という原因も対処も分からない文言が出ていたのを修正した。** こちらは非同期コマンドの安全網が例外を受けるためアプリが落ちることはなかったが、`RegexMatchTimeoutException` を専用に捕まえていなかったため、他のあらゆる想定外の不具合と同じ扱いになっていた。エディタ内検索と同じ文言を出すようにし、あわせて**最初にタイムアウトを検出した時点で検索全体を打ち切る**ようにした（同じパターンはどのファイルに対しても同じように破滅的バックトラックを起こすため、1ファイル2秒ずつ待ちながら全ファイルを走り続けても結果は得られず、利用者を待たせるだけのため）。一括置換側も、タイムアウトしたファイルを「失敗」として理由つきで記録し、他のファイルの置換は続けるようにした（照合の時点で失敗しているのでそのファイルは1文字も書き換わらない）
- **同種の穴の点検で見つかった `.gitignore` 由来の固まりも塞いだ。** `.gitignore`（およびプロジェクト設定の追加除外）の各行も、利用者が書いた文字列から正規表現を組み立てている。`**/` が何段も続く行は `(?:.*/)?` の連続になり、一致しない深いパスに対して破滅的バックトラックを起こしうるが、こちらは**タイムアウトすら付いていなかった**ため、検索やコンテキスト収集の途中で UI スレッドが**永久に固まる**（実際に、修正前のコードでは検証用のテストが5分経っても終わらないことを実測で確認した）。検索欄と同じ2秒のタイムアウトを付け、タイムアウトした規則は「このパスには一致しなかった」として読み飛ばすようにした。除外規則1行の書き方のせいで機能全体が止まるより、その1行だけが効かなくなるほうが被害が小さいという判断
  - なお、これ以外に `Regex` を使っている箇所（Markdownの描画・構文強調・色リテラルの検出・パッチの解析など）はすべて**開発者が書いた固定のパターン**を1行程度の入力に当てるもので、入れ子の量指定子を含まず、利用者の入力がパターンそのものになることも無いため、同じ危険は無いことを確認した

### 修正（性能）
- **SEARCH部が一致しなかったときの「類似度による照合」（段階5）が、大きなファイルで数十秒〜数分止まる問題を修正した。** 点検での実測は、2,000行のファイルにSEARCH 120行で8.78秒、20,000行にSEARCH 100行で52.24秒、**100,000行にSEARCH 100行では281.35秒**（4コアのLinuxコンテナ・Release）。段階5は**段階1〜4で一致しなかったときにしか動かない**ため、「一致しなかったときこそ何分も待たされる」という最悪の性質になっていた。「10万行のファイルでも編集が滞らない」という約束の裏で、この経路だけ穴が空いていた
  - 【原因】 全開始位置 ×（窓長のゆらぎ 2×20+1通り）× 編集距離DP（SEARCH行数 × 窓長）を、枝刈りも上限も無しで総当たりしていた。窓長のゆらぎ幅はSEARCH行数に比例して最大20まで伸びるため、SEARCHが長いほど二乗で効いた
  - 【修正】 **結果を1件も変えない枝刈り**を二段構えで入れた。(1) 行の多重集合の共通部分から類似度の**上限**が求まる（編集距離 ≥ max(n,m) − 共通行数）ので、上限がしきい値にも暫定ベストにも届かない窓はDPを回さずに捨てる。開始位置ごとの粗い判定はスライド窓で1開始位置あたり一定時間にした。(2) 採用され得る編集距離の上限が分かっているので、DPをその幅の帯に限り、行の最小値が上限を超えた時点で打ち切る。さらに、しきい値が低い呼び出し（AIへの再依頼文を組み立てる「リカバリ支援」はしきい値0で最も似た箇所を探す）でも枝刈りが効くよう、粗い上限の高い開始位置を先に数件評価して枝刈りの下限を底上げしている
  - 【実測（同じ条件で測り直し）】 2,000行×120行 8.78秒→**0.00秒**、10,000行×40行 2.39秒→**0.00秒**、20,000行×100行 52.24秒→**0.00秒**、100,000行×100行 281.35秒→**0.01秒**
  - 【精度は落としていない】 段階5の価値は「AIの出力が少しずれても拾う」ことにあるため、速くするために当たらなくなっては本末転倒である。修正前のアルゴリズムをテスト側にそのまま残し、無作為な入力（同点が大量に出るよう語彙を絞った入力を含む）に対して、**採用される開始行・行数・類似度が、同点のときの勝者まで完全に一致する**ことを総当たりで固定した
  - 【安全網】 「どの窓もSEARCH部の並べ替えになっている」ような、枝刈りが本質的に効かない病的なファイルのために、DPの計算量に予算を設けた。使い切ったら段階5を打ち切り、**「ファイルが大きく似た行が多いため、類似度による照合（段階5）を途中で打ち切りました。SEARCH部を短くするか、範囲指定（アンカー）での指定をAIへ依頼してください」という理由をE101に添える**。黙って打ち切ると、利用者にはGraftが見落としたようにしか見えないため
  - 【今回見送ったこと（正直な報告）】 プレビュー（ドライラン）に「中止」ボタンを付ける案は見送った。上記の修正で最悪でも1ブロックあたり1秒弱で終わるようになり、中止という操作自体の必要性が下がったためで、UI側（`ApplyEngine.DryRunAsync` へ`CancellationToken`を渡す配線と画面の変更）には手を入れていない

### 修正（表示・文言）

実画面での点検で見つかった、機能そのものは動いているのに**画面の文字だけが利用者を誤った判断へ導く**不具合をまとめて直した。いずれも修正後の表示を実際にアプリを起動して確認している。

- **対処文どおり `OCCURRENCE=1` を書いても、まったく同じ E102 で止まってしまう問題を修正した。** 同じファイル・同じSEARCHに対して `OCCURRENCE=2` は適用できるのに、最も自然な `OCCURRENCE=1` を書くと E102「複数箇所にマッチ／OCCURRENCE を指定してください」が**同じ対処文つきで**返っていた。**言われたとおりに直した利用者が、画面からは絶対に分からない理由で詰む**状態だった
  - 【原因】 出現指定（`OccurrenceSpec`）の `Index` が「既定値1の `int`」で、既定かどうかを `Index == 1` で判定していた。そのため「OCCURRENCE 未指定」と「明示的に1番目を指定」がまったく区別できなかった
  - 【修正】 `Index` を `int?`（未指定なら `null`）に変え、「値が入っている＝明示指定」を型で表すようにした。フラグを1つ足す案もあったが、`new OccurrenceSpec { Index = 2 }` のように**立て忘れた生成が黙って「未指定」に化ける**危険が残る（実際、既存テストにその書き方があった）ため、立て忘れようのない形を選んだ
  - あわせて E102 の対処文を「SEARCHマーカー行に `<<<<<<< SEARCH OCCURRENCE=1` のように何番目を対象にするか書いてください（すべて置き換えるなら `OCCURRENCE=ALL`）。SEARCH部に前後の行を足して一意にする方法でも解決できます」へ直し、エラーの詳細にも「`OCCURRENCE=1` 〜 `OCCURRENCE=N` を指定できます」と**指定できる範囲**を添えるようにした
- **プロジェクトのフォルダが見つからないだけで「E404 設定・履歴データの破損」と告げていた問題を修正した。** フォルダを別の場所へ移動した、ネットワークドライブが未接続、というだけで**何も壊れていないのに**「設定・履歴データの破損」と表示され、しかも E404 の対処文は「退避のうえ再生成しました」で、この状況にまったく当てはまらなかった。利用者は設定や履歴が失われたと誤解する
  - 【修正】 専用のエラーコード **E213** を新設した。対処文は「フォルダを移動した場合は、プロジェクトペインでこのプロジェクトを右クリックし『場所を変更』から選び直してください。ネットワークドライブ上にある場合は接続を確認してください。もう使わない場合は同じ右クリックメニューから削除できます」とし、**利用者が実際に取れる行動**を書いた
  - 同じ長いパスが1行に2回出て読みづらかった点（表示側がパスを前置するのに、本文にも書いていた）も直した
  - 起動時のレポートに出るのは「コード＋要約＋パス＋詳細」だけで、対処文は「詳細をコピー」の中にしか入らない。**画面に出る文言だけで次の行動が分かる**よう、対処を詳細側にも持たせている（実際に起動して画面で確認した）
- **履歴の「Nファイル」が、実際に変更した件数と食い違っていた問題を修正した。** 統計はドライラン時点の全ブロックから数えており、**適用できなかったブロックやチェックを外したブロックまで数に入れていた**。1ファイルだけ適用したリビジョンが「2ファイル +1 -1」と表示され、履歴を後から見た利用者が「2ファイル変えた」と誤解し、復元の影響範囲の判断も誤る状態だった。リビジョン確定時に、実際に記録したエントリから数え直すようにした
- **更新の「最終確認」が、失敗した確認も成功と同じに見せていた問題を修正した。** 確認が3回連続で失敗しても画面には「最終確認: 2026/09/06 06:26」とだけ出ており（失敗はログのwarnにしか残らない）、「確認した＝最新だった」と読めてしまっていた。起動時の確認の失敗は画面に何も出さない方針のため、**オフラインが続くと利用者は何日でも更新が止まっていることに気づけなかった**。今後は「最終確認: 2026/09/06 06:26（確認できませんでした）」のように成否を併記する
- **設定の「JSON直接編集」で保存に失敗したとき、.NETの英語メッセージがそのまま出て、しかも右端で見切れていた問題を修正した。** 実際の表示は `JSONを解析できませんでした: '@' is an invalid start of a value. Path: $ | LineNumb…` で、唯一役に立つ行番号が切れて読めなかった
  - 「E406 JSONとして読み取れません: 3行目12文字目のあたりが不正です（その位置の文字: 「@」）。カンマ・引用符・波かっこ（{}）の対応を確認してから、もう一度「検証して保存」を押してください。」のような日本語へ変換する。位置は例外の公開API（行番号・バイト位置）と入力テキストから自前で求めており、英語のメッセージ文面を解析してはいない（.NETの版で変わりうるため）
  - エラーの表示先が横並びの領域にあり、折り返し指定を書いても**一切折り返せない**状態だったため、独立した行へ移した
- **ファイル横断検索で不正な正規表現を入れたとき、`正規表現が不正です: Invalid pattern 'foo(b…` と英語が出て右端で見切れていた問題を修正した。** 日本語の一言（「かっこ『()』や角かっこ『[]』の対応、末尾の『\』の有無を確認してください」）へ置き換え、表示先にも折り返しを付けた。エディタ内検索の「置換文字列が不正です」も同様に直した
- **適用後フックが失敗したとき、「終了コード 1」としか出ず、何が失敗したのか分からなかった問題を修正した。** 標準出力・標準エラーは内部では取れていたのに画面へ一切出していなかったため、**出力の末尾5行を併記する**ようにした（全体の行数も明示する。ビルド出力は数千行になりうるため行数と1行の長さの両方で抑える）
  - あわせて、`HookResult.Output` のコメントに「manifest には保存しない」と書いてあったのに**実装はビルド出力の全文を `manifest.json` へ永続化していた**食い違いを解消し、書いてあるとおり保存しないようにした。ビルド出力には絶対パス・環境変数・トークンが混じりえて、`manifest.json` はリビジョンごとに残り続けるため
- **履歴の復元に失敗したときの通知に、OK＋キャンセルの確認ダイアログを使っていた問題を修正した。** 単なる通知なのに「キャンセル」が並び、押しても何も起きなかった（戻り値を捨てているためどちらでも同じ）。他の失敗通知と同じ、OKのみのメッセージへ直した
- 部分適用の完了メッセージ「（1件は適用できませんでした）」に理由が無かったため、「適用できなかった理由は、接ぎ木パネルの各ブロックに赤字で残っています。」を添えるようにした
- 更新の配布物ダウンロードに失敗したとき、.NETの英語の内部メッセージがそのままダイアログへ出る経路があったため、日本語の説明を先に出すようにした（通信断は「ネットワークに接続できませんでした。接続状態を確認してから、もう一度お試しください。」）。原因調査のため、原文は従来どおり「（詳細: ...）」として後ろへ残している


### 改善（配色・見やすさ）
- **UI点検で見つかった、9テーマぶんの配色コントラスト不足をまとめて直した。** すべてテーマの色トークン定義値からWCAGコントラスト比を実測したうえでの対応で、対象は次のとおり
  - **無効化されたボタン・チェックボックスが薄すぎて読めない問題**（既定ライト・セピア・Solarized Light・GitHub風の4テーマ）。`TextDisabledColor` を同じ色相・彩度のまま濃くし、いずれも本文背景に対し3:1以上を確保した（例: Solarized Lightは実測1.35:1→3.15:1）。「押せない」薄さ自体は残したまま、コマンドバーの「キューへ追加」「適用」や接ぎ木パネルの操作ボタンが判読できるようにした
  - **補助テキスト（`TextSecondary`）が基準未満だった問題**（Solarized Light・セピア・Nightの3テーマ）。同じく色相・彩度のまま濃く（暗いテーマは明るく）し、本文背景・面背景の両方で4.5:1以上を確保した。選択したリスト項目の上でも4.5:1を満たすかを別途確認し、既定ダーク・Solarized Darkも選択行だけ基準未満だったためあわせて調整した。**Solarized Darkだけは選択行の上での4.5:1と「本文より薄く見える」階層の両立が難しく、階層を優先して4.24:1（旧3.04:1からは改善）に留めている**
  - **チェックボックスのチェックが見えない問題**（セピア・Solarized Light）。チェック時は箱の背景をアクセント色で塗り、チェック自体を背景色で描く一般的な作法に変更し、線の細さではなく塗りの面積でオン/オフが分かるようにした
  - **フォーカスリングが見えないテーマがある問題**（Solarized Light・セピア）。アクセント色とは独立した専用トークン（`FocusRing`）を9テーマに新設し、この2テーマだけ既存の高コントラスト色（区切り線ホバー強調と同じ色）を割り当てた。他の7テーマはアクセント色のままで見た目は変わらない
  - **パネルの境界線がほぼ見えない問題**（既定ライト・既定ダーク・Solarized Light・セピア・GitHub風の5テーマ）。「主張しない区切り」の意図を保ったまま、コントラスト比を1.0〜1.3程度から1.7台へ引き上げた
  - **警告・成功の色が明るいテーマで弱い問題**（既定ライト・Solarized Light）。色相を変えずに濃くし、警告文は4.5:1以上、アイコンは3:1以上を確保した
  - **フォントプレビューが画面で一番薄い色で描かれていた問題**（設定画面の「本文フォント」「等幅フォント」プレビュー行）。本文と同じ濃さの文字色に直し、選んだフォントの実際の見え方が判断できるようにした
  - **Solarized Darkで選択行の本文がやや薄い問題。** 選択行の背景色を同じ色相のまま濃くし、本文・補助テキストの両方の見やすさを同時に底上げした
  - **チュートリアルの覆いがテーマを無視して常に黒だった問題。** 覆い専用トークンを9テーマに新設し、明るいテーマは不透明度を約54%から約40%へ下げた（暗いテーマは従来どおり）
  - なお、空状態のアイコン等、色が薄い表示に加えて `Opacity` も重ねて掛かっている箇所は、今回のトークン修正だけでは実効コントラストが3:1を大きく下回ったままであることを確認した。表示位置の調整と同時に別途手を入れる作業と競合する可能性があるため、今回は色トークンの修正に留め、`Opacity` 自体の見直しは今後の課題として残す
  - 9テーマ×主要項目のコントラスト比（修正前後）とスクリーンショットによる目視確認の詳細はPR説明を参照

## 1.0.14

### 修正
- **マップ済みネットワークドライブ（`Z:\...`）やネットワーク共有上のプロジェクトで、既存ファイルの変更がすべて E210（ファイルが見つからない、またはこの環境から読み取れない）になる不具合を修正した。** 実機ログで、`projects.json` の保存値は `Z:\営業部\...\MAI-History` と正常だったのに、`PathGuard.NormalizeRoot` の戻り値が `C:\追加\Graft\UNC\gfs\inaden\営業部\...\MAI-History`（`C:\追加\Graft` は Graft.exe を置いたフォルダ）になっていた。保存データは無実で、**壊れていたのは実行時のパス解決**だった
  - 【原因】Graft はプロジェクトルートを組み立てる際、途中のフォルダがシンボリックリンク／ジャンクションなら実体まで解決している（ルート外への脱出を防ぐため）。この解決に使う .NET の `Directory.ResolveLinkTarget(path, returnFinalTarget: true)` は、内部で Win32 の `GetFinalPathNameByHandle` を呼ぶ。この API は**常に拡張表記**を返し、対象がネットワーク上なら `\\?\UNC\サーバ名\共有名\...` になる。ところが .NET 8 の実装（`System.IO.FileSystem.GetFinalLinkTarget`）は、**戻り値の先頭4文字を無条件に切り落とす**。`\\?\C:\...` → `C:\...` を意図した処理だが、`\\?\UNC\...` に対しては `UNC\サーバ名\共有名\...` という**相対パスに見える文字列**を生んでしまう
  - Graft はこれまで `FileSystemInfo.FullName` を使っていたが、`FullName` は `Path.GetFullPath` 済み、つまり**カレントディレクトリ（＝exe のフォルダ）と連結済み**である。そのため `C:\追加\Graft\UNC\gfs\inaden\営業部` が出来上がり、以後のすべてのファイルがこの無関係な場所の下で探されて E210 になっていた
  - **ローカルのプロジェクトでは再現しない**（ローカル対象なら `\\?\C:\...` から4文字落として `C:\...` となり .NET の処理は正しい）。実機で「ローカルのプロジェクトは正常、ネットワーク上だけ全滅」だった報告と一致する
  - なお v1.0.7 の `LongPath` に「拡張UNC表記の先頭4文字だけが何らかの理由で失われる」と書いて発生源不明としていた形は、**ここが発生源だった**
  - 【修正】(1) リンクの実体としては `FullName`（連結済みで手遅れの値）ではなく、`FileSystemInfo.ToString()` が返す**生の値**をまず見て、`UNC\...` なら `\\...` へ復元し、`\\?\` は剥がす。(2) 実体解決が相対パスを返したら**その時点で異常**とみなし、組み立てを打ち切って元のパスをそのまま使う（カレントディレクトリと連結させない）。(3) `Path.GetPathRoot` がルートを取れない入力は、組み立てても相対パスにしかならないため何もしない。(4) `NormalizeRoot` の最終結果が絶対パスであることを確かめ、そうでなければリンク解決前の値へ後退する。(5) ルート内判定の直前で拡張表記を通常表記へ戻す
  - 【ログ】拡張表記や相対パスへの転落を検知したら、何が起きて何を採用したかをログへ記録するようにした（`path-guard`）。「いつのまにか直った／壊れた」で終わらせないため
  - 【検証の限界（正直な報告）】.NET 側の該当実装は、win-x64 ランタイム 8.0.29 の `System.Private.CoreLib` を逆コンパイルして確認した。ただし**実機でこの経路を実際に通ること（ネットワーク上のリンク解決と `GetFinalPathNameByHandle` の戻り値）は Windows 実機でしか確認できない**。開発環境が Linux のため、テストで固定したのは文字列処理として切り出した判断（`PathGuard.SanitizeLinkTarget` / `PathGuard.ResolveRealPathCore`）のみ
- **マップ済みネットワークドライブ（`Z:\...`）上のプロジェクトで、プロジェクト直下の普通のファイルにパッチを当てようとすると `E201 パスがルート外（theme.js）: シンボリックリンク経由でルート外を参照しています` になる不具合を修正した。** 対象は `..` も絶対パスも含まない、エクスプローラにも表示されている実在のファイルだった。v1.0.14 の修正（ルート解決の化け）は正しく効いており、その先で露出した別の不具合である
  - 【実機ログで確定したこと】 同じ `projects.json` の値に対して、`PathGuard.NormalizeRoot` の戻り値（`environment` ログ）が**時刻によって2通りに揺れていた**。`14:03:09` は `\\gfs\inaden\営業部\...`（UNC共有）、`14:03:57` は `Z:\営業部\...`（ネットワークドライブ）、`15:37:23` は同じプロジェクトが再び `\\gfs\inaden\営業部\...`。`Z:` は `\\gfs\inaden` へのマップなので、**同じ場所を指しているのに文字列としての表記だけが揺れている**。E201 が出たのはルートが `Z:` 表記だった時間帯（`16:17` 台）だけで、両者の表記が揃っていた時間帯（`15:39`・`15:46`・`15:51`）の適用は3回とも成功している
  - 【原因】 プロジェクトルート `_root` は、`PathGuard` を作った時点で**1回だけ**リンクの実体解決をかけた値である。ところが `Resolve` は、ファイル1件を解決するたびに結合後のパスを**ドライブルートから丸ごと**実体解決し直していた。この「構築時の1回」と「毎回」の結果が食い違うと、同じ場所を指しているのに前方一致（ルート内判定）が外れ、ルート内のファイルが「リンク経由でルート外」と誤判定される。実機ではルートの途中の構成要素が `Z:\営業部` のままだったり `\\gfs\inaden\営業部` に化けたりしていたため、これが起きた
  - 【なぜ揺れるのか（分かっていないこと）】 v1.0.14 で追加した `path-guard` イベントは実機ログに**1件も出ていない**。つまり「実体解決が絶対パスにならず後退した」経路は通っておらず、揺れているのは **`Directory.ResolveLinkTarget` がその構成要素を「リンクである」と報告するかどうかそのもの**である。ネットワークの状態やキャッシュに依存している可能性が高いが、**なぜ揺れるのかは特定できていない**。Windows 実機かつネットワーク越しでしか起きないため、開発環境（Linux）では再現できない。**この揺れ自体は塞げていない**
  - 【修正】 揺れを前提に、**「同じ土俵で比較する」形へ変えた**。(1) リンクの実体解決の起点を `_root` そのものにし、**ルートより下の構成要素だけ**を辿るようにした。結合後のパスは「絶対パスでない」「`..` を含まない」ことを確認したうえでルートに連結した値なので、ルートより上の構成要素は利用者やAIが与えた相対パスの影響を受けない。したがって毎回辿り直す理由が無く、辿り直さなければ表記が入れ替わる余地も無くなる。(2) 第二の砦として、前方一致が外れた場合にだけ「ルートの実体」を求め直し、そちらの配下かどうかも確かめる。同じ場所を指す別表記で返ってきても同じ結論になる
  - 【安全機構は緩めていない】 通すのは「ルートの配下」か「ルートの実体の配下」だけで、**本当にルート外を指すリンクはそのどちらからも外れる**ため従来どおり E201 で拒否される。ルート配下のリンクを実体まで解決して脱出を検知する動作そのものは変えていない。「ルート外を指すリンクが拒否されること」「表記が食い違っても誤って拒否しないこと」の両方をテストで固定した
  - 【副次的な効果】 ネットワーク上のプロジェクトでは、ファイル1件を解決するたびにルートの深さぶん（実機の例では8段）の存在確認・属性照会が SMB 越しに発生していた。エクスプローラの一覧はこれをファイル数ぶん繰り返すため、往復回数が「ファイル数×ルートの深さ」から「ファイル数×相対パスの深さ」へ減る
  - 【ログ】 E201 でリンク経由のルート外参照として拒否したとき、**そのときの `_root`・結合後のパス・実体解決後のパス・どの構成要素が何へ解決されたか**をログへ残すようにした（`path-guard`）。v1.0.14 のログは「絶対パスへ解決できなかった」異常時にしか出ないため、今回の調査では実際の文字列を `environment` 行から推定するしかなかった。同じ推定を二度としないための追加であり、記録するのはパスだけでファイルの内容は含まない
  - 【採らなかった案】 `WNetGetConnection` で `Z:` → `\\gfs\inaden` を引き当てて表記を揃える案は採らなかった。(a) Windows 専用の P/Invoke が増え、Linux の開発環境・CI では中身を一度も実行できない（検証できないコードが増える）。(b) 実機で観測された揺れは**ドライブ文字の部分だけでなく途中の構成要素でも起きうる**ため、ドライブ文字の対応表だけでは塞ぎ切れない。(c) ファイル1件ごとに呼ばれる同期的な検査の中でネットワーク API を叩くことになり、切断時の待ちや資格情報の要求という新しい失敗要因が増える。(d) 今回の修正で「そもそも表記が入れ替わる場所を通らない」ようにできるため必要が無い。将来この設計で塞げない揺れがログに出たら、テスト可能な形（インタフェース越し）で再検討する
  - 【対象のファイル自体がリンクだった可能性は否定できる】 「`theme.js` や `settings.js` そのものがルート外を指すシンボリックリンクで、E201 が正しい判定だった」という可能性も検討したが、**同じファイルが、表記が揃っていた時間帯には問題なく適用できている**（`15:46` に `settings.js` を含む3件、`15:51` にも3件が成功）。ファイル側がルート外を指していたのなら、その時間帯も同じく拒否されていたはずである。したがって食い違いはルート側の構成要素で起きていたと考えてよい
  - 【検証の限界（正直な報告）】 `Z:` と `\\gfs\inaden` が同じ場所を指すこと、`ResolveLinkTarget` の報告が揺れること自体は **Windows 実機でしか再現できない**。テストで固定したのは、揺れが起きたときに前方一致がどう転ぶかという判断（走査ロジックと文字列比較）だけである。また `.github/workflows/ci.yml` は `runs-on: ubuntu-latest` のため、`OperatingSystem.IsWindows()` で守った検証は **CI では中身が実行されない**（テスト自体は成功として数えられるが、Windows 専用の表明は評価されない）

## 1.0.13

### 追加
- **AIの回答を「標準SEARCH/REPLACE形式」でも受け付けるようにし、AIへの指示文（プロンプトテンプレート）の既定もこの形式へ切り替えた。** 多くのAIコーディングツール（Aider、Cline、Roo Code など）で使われている一般的な形式で、目印がGitの衝突マーカーそのものであるため、AIが学習済みで自然に出力できる。従来からの課題である「形式を指示してもAIが独自形式に従ってくれない」を、Graft側が形式に合わせにいくことで根本から減らす狙い
  - 受け付ける書き方は4種類。**部分修正**（パスだけの行 → `<<<<<<< SEARCH` / `=======` / `>>>>>>> REPLACE`）、**新規ファイルの作成**（`<<<<<<< NEW_FILE` 〜 `>>>>>>> END_FILE`）、**全面書き換え**（`<<<<<<< WHOLE_FILE` 〜 `>>>>>>> END_FILE`）、**ファイルの削除**（`<<<<<<< DELETE_FILE` 〜 `>>>>>>> END_FILE`）
  - 【調べた結果】実装にあたり、この形式を広めたAiderの公式ドキュメントと本体のプロンプト実装を実際に参照して裏を取った。**素の標準形式では、新規ファイルの作成は「SEARCH部を空にする」で表すのが本来の作法**であり、`NEW_FILE` / `WHOLE_FILE` / `DELETE_FILE` というマーカーは標準には存在しない（これらは運用ルール側の独自拡張）。ファイルの削除・改名も素の標準の守備範囲外だった。そこでGraftは、**空SEARCHによる新規作成と、`NEW_FILE` 等のマーカーによる書き方の両方**を受け付ける。どちらで届いても同じ結果になるので、AIがどちらの流儀で返してきても取り込める
  - パスは必ずプロジェクトからの相対パスとして扱い、`../` で上の階層へ出るパスや絶対パス（`/etc/...`・`C:/...`）は従来のGraft独自形式とまったく同じ検査で拒否する。プロジェクト外への書き込みが起きないことを、テストでも固定した
  - AIが「直す場所を正確に指定できない」と判断したときに返す `NEED_MORE_CONTEXT` の1行を、専用のエラーコード（E710）として区別するようにした。従来の扱い（E001「ブロックが存在しない」）のままでは「AIが変な出力をした」としか見えず、実際には「対象ファイルをもう一度渡せば解決する」という別の状況であることが伝わらないため。クリップボード監視でもこの1行を検知するので、コピーしたのに何も起きない、という状態にならない
  - **従来のGraft独自形式・unified diff形式は今までどおり読み取れる。** 形式の判別は、両形式で共通の `<<<<<<< SEARCH` ではなく「Graft独自形式にしかないヘッダ（`<<<< FILE:` など）があるかどうか」で行うため、履歴やパッチキューに残っている既存のパッチの読み取り方は一切変わらない。既存の自動テストが1件も落ちていないことを確認済み。なお1回の回答に両方の形式が混ざっている場合は、後方互換を優先してGraft独自形式として解析する（この場合、標準形式で書かれたブロックは取り込めず解析に失敗する）ため、AIへの指示文でも「1回の出力で2つの形式を混ぜないこと」を明示している
- プロンプトテンプレートに、従来のGraft独自形式版（「初回用（Graft独自形式）」「継続用（Graft独自形式）」「修正依頼（Graft独自形式）」「新規実装（Graft独自形式）」）を追加した。既定は標準SEARCH/REPLACE形式に切り替わるが、**従来の指示文もそのまま一覧から選べる**。ファイルの移動・改名、フォルダの作成、末尾や先頭への追記、変更全体の要約（summary）はGraft独自形式にしかできないため、必要な場面では引き続きこちらを使える
- 起動時、日本語（ひらがな・カタカナ・漢字）を1文字も描画できるフォントが環境に見つからない場合、その旨をログと起動時の確認ダイアログで警告するようにした（E705）。判定は「Noto Sans CJK JP」等の特定のフォント名との一致ではなく、実際に「その文字を描画できるフォントがシステムにあるか」をOSのフォントサブシステムへ横断的に問い合わせる方式のため、フォールバック列（取扱説明書のフォント設定を参照）に挙げた名前と実際にインストールされているフォントの正式名がわずかに違うだけの環境（例: 「IPAGothic」を「IPAexGothic」と取り違える等）で誤って警告することが無い
- タスクトレイ常駐・自動起動がこの環境では利用できない場合に、その事実をE706としてログへ記録するようにした。設定画面側の案内（該当項目の無効表示）はこれまでどおりで見た目の変化は無いが、実際にどの機能がどの理由で無効になったのかを起動時のログから追えるようになった
- OSのハイコントラストモードを検出できるようにした（E709）。Windowsは`SPI_GETHIGHCONTRAST`、Linuxは対応するデスクトップ設定（GNOMEの`high-contrast`設定）があればそれに追従し、無い場合は何もしない。Graftはプラットフォームや検出結果によらず自前の配色トークンを維持する方針（取扱説明書のテーマ設定を参照）のため配色は切り替えないが、検出できたことは診断用にログへ記録する

### 改善
- **10万行級のファイルでスクロールバーのつまみをドラッグしたとき、表示がマウスに追い付かない問題を大きく改善した（課題#73）。** 10万行のファイルではつまみ1pxが文書の2,244px（約128行）に相当し、1px動かすたびに画面上の46行がすべて作り直される。この1回分のレイアウト費用が、素のエディタ基盤（AvaloniaEdit）の8.3msに対しGraftでは17.0ms（2.12倍）かかっており、ドラッグ中に描画が追い付かなくなっていた。今回これを**10.8ms（1.28倍）**まで下げた。画面の描画まで含めたフレーム全体でも、基準比の上乗せが+13.4ms→**+2.7ms**になっている（実測はいずれも修正前後を同一手順・同一環境で交互25組計測した中央値）
  - 【真因】 上乗せ8.7msのうち**5.1msが折りたたみ**（残りは主に構文強調）だった。エディタ基盤の折りたたみ機能は、画面へ1行描くたびに「この位置より後ろで最初に畳まれている範囲はどこか」を先頭から順に探す作りになっており、**どこも畳んでいないときは範囲の一覧を最後まで舐めきる**（＝一番普通の状態が最悪ケース）。10万行のC#ファイルでは折りたたみ範囲が20,000個できるため、これを46行分繰り返していた。文書のどこをドラッグしているかで費用が変わる（文書の2%地点で+10.8ms、25%で+8.5ms、50%で+5.3ms、75%で+1.3ms＝残りの範囲数に比例する）ことも実測で裏付けた。素のエディタ基盤はどの位置でも一定である
  - 【対処】 **1つも畳まれていない間は、折りたたみの描画部品をエディタ基盤の描画パイプラインから外しておき、1つでも畳まれたら戻す**ようにした。どこも畳んでいないとき、この部品は「何も描かない」ことが確定しているため、外しても表示は完全に同一になる（2,000行のファイルを500行目まで送った状態で描画結果のPNGを比較し、1バイトも違わないことを確認済み。自動テストとしても固定した）。結果、折りたたみによる上乗せは**+5.06ms→−0.03ms**となり、ドラッグ位置による差も消えた（2%地点で+0.09ms、75%地点で−0.46ms）
  - 折りたたみ機能そのものに手を入れる変更のため、畳む・展開する・すべてのコメントブロックを畳む・折りたたみレベルの指定・タブ切替後、のそれぞれで**実際に行が隠れること**を自動テストで確認している（マーカーを押している最中のフレームまで含めて検証）。設定項目は増えておらず、利用者から見た折りたたみの操作・見た目は一切変わらない
  - あわせて、構文強調が文字色のブラシと斜体の書体を**1トークンごとに毎フレーム作り直していた**のをやめ、テーマごとに1度だけ作って使い回すようにした。ただし正直に書くと、これ単体でのレイアウト時間の短縮は測定誤差の範囲だった（上乗せ+2.03ms→+2.00ms）。はっきり効いたのはメモリの割り当て量で、ドラッグ60ステップ分が116.6MB→110.9MB（1フレームあたり約95KB減）になっている。ドラッグ中のGCの圧力が下がる分、時間の中央値ではなく「時々出る引っかかり」に効く種類の改善である
  - 【副次的な効果】 本版で修正した課題#82（タブ切替時にまれに出る`Invalid document`の未処理例外）は、この`Invalid document`を投げる箇所がエディタ基盤の中で**折りたたみの描画部品ただ1箇所**しか無い。今回の変更により、**どこも畳んでいない通常状態ではその経路自体を通らなくなった**（1つでも畳んでいれば従来どおり通るため、1.0.13の対処はそのまま残してある）。この2つの状態の違いも自動テストで固定した
- 行の折り返しを有効にしているとき、折り返された2行目以降を、その行のインデント位置まで字下げして表示するようにした（課題#72）。深く字下げされたコードの長い行が折り返されると、続きが画面の左端から書き出されてしまい、どこからどこまでが1行なのか・どのブロックに属する行なのかが読み取りにくかった。今後は続きの行が元の行のインデント位置から始まるため、インデントガイド（縦線）とも重ならず、折り返しても構造が目で追える
  - 【なぜ今まで効かなかったか】 この動作はエディタ基盤（AvaloniaEdit）に`InheritWordWrapIndentation`（既定オン）という設定として最初から備わっているが、実際には一度も効いていなかった。原因は基盤側の2箇所にある。①AvaloniaEdit 11.1.0（Graftが性能要件のために固定しているバージョン。11.4.0でも未修正）は、行の書式を組み立てる際に「段落の1行目か」を示すフラグを設定し忘れており、字下げ量を計算するコード自体が一度も実行されない。②仮に①が直っても、その下のUI基盤（Avalonia 11.2.3、最新の11.3.20も同様）のテキスト整形器が、字下げ量を受け取るプロパティを一切参照していない。したがってGraft側で設定値を変えるだけでは実現できず、Graftが自前の整形器を挟む形で実装した（`src/Graft/Editor/WrapIndentSupport.cs`のコメントに、該当するクラス名・メソッド名・行番号まで含めた調査結果を残してある）
  - この実装はエディタ基盤の内部（非公開）に触れるため、将来基盤側の作りが変わって触れなくなった場合は、アプリを落とさずに「字下げが効かないだけ」の従来どおりの表示へ静かに戻る。その事実はログへ記録されるので、気付かないまま劣化し続けることはない（回帰テストで確認済み）
  - 右横書き（アラビア語・ヘブライ語など）を含む行では字下げを適用せず、従来どおりの表示にする。双方向テキストでは文字の並びが視覚的に入れ替わるため「行頭の空白の幅」を正しく測れず、誤った位置へ字下げするより従来どおりの方が安全なため
  - 設定項目は増やしていない（常時オン）。エディタ基盤でも既定でオンとされている動作であり、「1行として続いているものを1行として見せる」という表示の正しさに関わる挙動のため、利用者に選ばせる性質のものではないと判断した

### 修正
- 起動のたびに必ず走る起動時検証（back/配下の実体走査によるリビジョン番号の補正）が、その完了を待たずに他の操作（プロジェクトの登録・削除など）が行われた場合、古い一覧のスナップショットで上書きしてしまうことがある問題を修正した。「使い方を学ぶ」チュートリアル終了後にプロジェクトが一覧から消えるという報告の調査で見つけたもので、リビジョン番号の補正は対象プロジェクト1件だけを更新する方式に改め、他の操作の結果を巻き戻さないようにした
- 折りたたみ有効時にタブを切り替えると、まれに`System.ArgumentException: Invalid document`という未処理例外が発生する不具合（課題#82）の入口を特定し、修正した。過去2回（課題#60・PR #33）「修正済み」としていたが実機ログで再発しており、今回さらに一段深い原因を突き止めた
  - 【真因】 `Editor.Document`を新しい文書へ差し替える処理は、AvaloniaEdit内部で`TextArea.Document`→`TextView.Document`の順に**同期的に**伝播したあと、キャレット位置を(1,1)へリセットする（`Caret.PositionChanged`が同期発火する）。折りたたみの再接続（古い`FoldingManager`を外して新しい文書で作り直す処理）は、この一連の流れの中で**一番最後に**発火する`TextEditor.DocumentChanged`イベントを契機にしていた。つまり「`TextView`の文書は新しいが、`FoldingManager`は古い文書のまま」という食い違った状態が、キャレットのリセット中に一瞬存在していた。このキャレットのリセット中（`Caret.PositionChanged`発火中）にOSレベルの再入（Windows実機でのIME通知・フォーカス変更・アクセシビリティ通知等に伴うネイティブメッセージポンプの入れ子呼び出しが典型例）で保留中の描画パスが処理されると、この食い違った状態のままレイアウトが走り、`Invalid document`が投げられる。実機ログの3件が起動直後（タブ復元中、複数回連続してDocumentが差し替わるタイミング）に集中していたことと符合する
  - この機序は自動テスト（headless）でも実際に再現できることを確認した（`tests/Graft.UiTests/EditorTests.cs`の「課題#82入口特定」テスト。`Caret.PositionChanged`のハンドラ内で`Dispatcher.UIThread.RunJobs()`を呼び、実機のネイティブメッセージポンプ再入と同じ効果をプラットフォーム非依存に再現している）
  - 【対処】 `Editor.Document`へ代入する**前**に、古い`FoldingManager`を先回りして取り外す（`FoldingSupport.PrepareForDocumentSwap`）よう`EditorPane`（タブ切替・空タブ化の両経路）を直した。これにより代入中のどの瞬間に再入が起きても、`FoldingManager`が存在しない状態のため例外の温床そのものが無くなる。同じ再入を起こしても例外が出ないことを回帰テストで確認済み
  - 併せて、この不具合が実機で発生した際に握りつぶした（あるいは記録した）回数を、既存の集計の仕組み（PR #45の`SuppressedExceptionTracker`）にも乗せるようにした。今後同種の未処理例外が起きた場合、終了時ログの集計で発生種類・回数がすぐ分かるようになる

### その他
- 折りたたみ機能の回帰テストを大幅に増やした（自動テストは単体1029件・UI783件の計1812件になった）。スクロールバーのドラッグ改善（課題#73）が折りたたみの描画経路そのものに手を入れるものであるため、「畳む・展開する・すべてのコメントブロックを畳む・折りたたみレベルの指定・再帰的な折りたたみ・タブ切替後・設定での無効化と再有効化」のそれぞれで**実際に行が隠れるところまで**確認する。加えて、テーマを切り替えたときに構文強調の色が追従することも自動テストで固定した（今回入れた色のキャッシュが古い色を握り続ける退行を防ぐため）
- 自動テストに、**行の折り返しを有効にした状態**での10万行の性能計測（読み込みと初回描画・スクロール）を追加した。Graftの既定は折り返しオンであるにもかかわらず、これまでの性能テストはすべて折り返しオフのエディタで計測しており、利用者が実際に使う経路の性能が一度も守られていなかった。あわせて、計測が本当に折り返し経路を通っていること自体を確かめる番人も入れた（幅やフォントの変更で折り返らなくなると、テストは通るのに何も守らない状態になるため）
- 仕様書（`docs/仕様書_Graft_v2.1.md`）を実装の実態に合わせて更新した。データ保存先の選択・Gitの自動コミット・エラー詳細のコピー・ログ参照・キーボードショートカットの追加など、既に実装済みだが記載が漏れていた機能を反映し、エラーコード表とテスト件数（自動テスト1708件）も実測値へ更新した。利用者への直接の影響は無い

## 1.0.12

### 改善
- 「起動時に更新を確認する」をオンにしていても、実際には前回の確認から24時間経つまで起動時の確認が行われず、チェックボックスの文言と動作が食い違っていた（利用者からの指摘）。今後は文言どおり、Graftを起動するたびに必ず確認する
- ただし、テーマ変更・データ保存先の移動・自動更新の適用後などGraft自身が再起動して立ち上がったときは、この「起動のたびに確認する」の対象から除いた。「起動のたびに確認する」のはあくまで利用者が自分の意思でGraftを起動したときの話であり、Graftの都合による再起動のたびにまで確認しに行く必要は無いため
- 自動更新のダウンロード中にGraftが強制終了・クラッシュすると、ダウンロード中だった一時ファイル（50MB超のZIPを含む一時作業フォルダ）がそのまま残り続け、次回以降も一切掃除されない不具合を修正した。今後は次回起動時に、作成から24時間以上経過した古い一時作業フォルダだけを自動的に削除する（実行中の更新を誤って削除しないための猶予時間）。また、更新を1回試みるたびに空の一時フォルダが1つずつ残り続けていた不具合もあわせて修正した

## 1.0.11

### 修正
- 自動更新に成功したあと、実行ファイルのフォルダに残る退避ファイル（`Graft.exe.old`等）が、「設定画面からデータ保存先をユーザーフォルダへ移動」している環境ではいつまで経っても掃除されず、更新のたびに積み上がっていく不具合を修正した。次回起動時の掃除処理が、退避ファイルの実際の置き場所（実行ファイルのフォルダ）ではなく設定・履歴等の保存先（データ保存先）を見に行っていたのが原因で、1.0.9で修正した自動更新のインストール先の取り違えと同じ種類の誤りが、この掃除処理だけ直っていなかった。ポータブル運用（データ保存先を移動していない環境）では両者が同じ場所のため、この不具合は発生しない

### 改善
- 「バージョン情報」タブに、更新を最後に確認した日時（「最終確認: yyyy/MM/dd HH:mm」、未確認なら「未確認」）を常時表示するようにした。起動時の確認は最新版のときに画面上へ何も表示されないため、確認したのか・24時間以内のため確認をスキップしたのか・そもそも確認できていないのかを見分けられなかった。あわせて、起動時・「今すぐ更新を確認」いずれの結果（最新だった・新版が見つかった・24時間未満のためスキップ・設定オフのためスキップ・通信に失敗）もログへ記録するようにした
- 「バージョン情報」タブの「チェック先URL」の入力欄を、ラベルの隣ではなくラベルの直下、「今すぐ更新を確認」ボタンの上に配置し直した。ラベル→入力欄→ボタンの縦並びにすることで対応関係が分かりやすくなり、入力欄もパネルの幅いっぱいまで使えるようになった

## 1.0.10

### 改善
- 設定画面「一般」タブにあった更新まわりの項目（起動時に確認するかどうか・チェック先URL・「今すぐ更新を確認」ボタン・ダウンロード進捗・現在のバージョン表示）を、「バージョン情報」タブへ移した。バージョン番号のすぐ下にまとまり、今のバージョンを確認しながら更新の状況も一目で見られるようになった

## 1.0.9

### 修正
- 設定画面から「データ保存先をユーザーフォルダへ移動」している場合、自動更新が必ず「ファイルの置き換えに失敗しました。Graft.exe を退避できませんでした」というエラーで失敗する不具合を修正した。自動更新が実行ファイルを置き換える場所と、settings.json等を保存する場所（データ保存先）を取り違えており、両者を別のフォルダにしている環境でだけ、実際には存在しないフォルダのGraft.exeを退避しようとして必ず失敗していた。ポータブル運用（データ保存先を移動していない環境）では両者が同じ場所のため、この不具合は発生しない

## 1.0.8

### 追加
- **自動更新機能を追加した。** 起動時（前回の確認から24時間以上経っていれば）と、設定「一般」タブの「今すぐ更新を確認」で、GitHubの公開リリースに新しいバージョンが無いか確認する。見つかった場合はダイアログで知らせ、同意した場合のみダウンロード・検証（SHA256）・置き換えを行い、確認のうえ再起動して反映する。書き込みが制限された場所（Program Files等）に置いている場合は自動更新をあきらめ、手動更新のためリリースページを開く案内を表示する。settings.json・プロジェクト一覧・バックアップ・ログ等、利用者のデータには一切触れない。設定「一般」タブで、起動時に確認するかどうか（既定オン）・チェック先URLを変更できる。確認・ダウンロードのとき以外、Graftは外部と通信しない（詳しい使い方は取扱説明書「更新」を参照）

### 修正
- AIの回答をコピーしやすいよう1つのコードブロックで丸ごと囲んで出力させたとき、パッチの中身自体に「```」を含む変更（Markdownファイルの編集など）を取り込むと、その```の行だけが黙って消えたまま適用されてしまう不具合を修正した（1.0.5から未修正のまま残っていた）。今後は囲み全体の外側だけを正しく見分けて取り除き、本文中の```はそのまま残す
- 上記に関連し、AIが指示文どおりに出力せず、本文の```と外側の囲みが衝突してしまい、囲みが途中で終わったと判断せざるを得ない場合は、黙って中身が欠けたまま取り込むのではなく、「パッチ本文に ``` が含まれているため、囲みが途中で閉じられた可能性があります」と案内し、書き直しを促すようにした

## 1.0.7

### 変更
- 設定「適用モード」の既定を「全件適用（All or Nothing）」から「部分適用可」に変更した。AIの回答の一部だけが実際のコードに当てはまらないのはよくあることで、これまでは既定のままだと、当てはまった変更が残っているにもかかわらず適用そのものがエラーになり、履歴にも記録されなかった。今後は既定のまま、当てはまった変更はきちんと適用されて履歴に残り、「元に戻す」もできる。当てはまらなかった変更があったことは、完了メッセージに件数を添えて知らせる。なお、すでに「全件適用」を選んでいる方の設定はそのまま引き継がれ、勝手に変わることはない

### 改善
- うまく動かないときの原因を調べやすくするため、起動時とプロジェクトを切り替えたときに、動作環境の要約（プロジェクトの場所・保存先・取り込みの挙動に関わる設定）をログへ記録するようにした
- 画面には出ないごく小さな不具合が裏側で繰り返し起きていないかを終了時のログから確認できるよう、発生回数の集計を記録するようにした（1度も起きていないものは表示されない）

### 修正
- ネットワーク上（共有フォルダ）に置いたプロジェクトを取り込むと、すべてのブロックが必ず失敗する不具合を修正した。プロジェクトの場所を記録した情報が壊れた形で保存されてしまい、まったく関係のない場所（Graftを起動したフォルダの中）を探しに行ってしまうことがあった。すでにこの状態で登録されているプロジェクトも、次に開いたときに自動的に直る
- 上記と合わせて、プロジェクトの場所を新しく登録・変更するときにも同じ壊れ方をしないよう防止策を加えた
- 「全件適用」を選んでいるときに一部の変更が当てはまらず中止した場合、これまでは中止した理由（設定によるもの）が伝わらず、原因不明のエラーのように見えてしまっていた。今後は「『全件適用』の設定のため中止しました。『部分適用可』に切り替えると、適用できる変更だけを書き込めます」という案内を表示し、個々の変更のエラーもあわせて示す
- 適用できる変更のチェックを画面上ですべて外した状態で「適用」を押すと、何も変更していないのに履歴だけが1件増えてしまう不具合を修正した。今後は「適用対象がありません」という案内を表示し、履歴は増やさない
- ファイルへの取り込み直後や、外部でファイルが書き換わった直後など、ファイルを読み込み直すタイミングで、まれにアプリが停止することがある不具合を修正した
- 折りたたみマーカー（コード左端に出る＋－の四角いボタン）が、環境によっては中の＋－がほとんど見えず、ただの塗りつぶした四角に見えてしまう不具合を修正した。9種類あるすべてのテーマで＋－がはっきり読めるようにした

## 1.0.6

### 追加
- 検索（Ctrl+F）でヒットした文字を見やすくした。一致した箇所には枠線付きの色を付け、いま選んでいる箇所は色も枠線も一段と目立つようにした。すべてのテーマで、色が見分けにくくても枠線の太さで現在地が分かる
- コードエディタのスクロールバーに、検索結果が文書全体のどのあたりにあるかを示す小さな目印を表示するようにした

### 改善
- 対象のファイルが見つからない・読み取れない場合に、専用のエラー（E210）で知らせるようにした。従来は「SEARCH部が見つからない」と表示され、原因が分かりにくかった。メッセージには Graft が実際に確認した絶対パスを添える
- 解析（ドライラン）のときに、対象ファイルごとの「絶対パス・存在の有無・行数」をログへ記録するようにした。うまく取り込めないときの原因調査に使える
- 長いパス用の内部処理を見直し、必要な長さのときだけ特殊な形式のパスを使うようにした（ネットワークドライブ上での不確実性に対する予防）

### 修正
- ファイルやフォルダを新しく作った直後に、ツリー上で自動的に選ばれないことがある不具合を修正した（自分自身の書き込みをファイル監視が拾ってしまう順序の問題）

## 1.0.5

### 改善
- 取り込みに失敗したとき、ブロックが閉じられていない箇所を具体的に案内するようにした。「1行目のブロックが閉じられていないまま、5行目で次のブロックが始まっています」のように、開始行と衝突した行の両方を示す
- AIへの指示文を改善した。ブロックの閉じ忘れが起きにくいよう注意点を加え、コードブロックの書き方（1行目・最終行に何を書くか、本文に ``` を含む場合の書き方）を具体的に示すようにした

## 1.0.4

### 改善
- コードエディタのスクロールバーを1.5倍の太さにして掴みやすくした
- スクロールバーのつまみに下限を設け、行数の多いファイルでも極端に短くならないようにした
- AIへの指示文（プロンプト）に「パッチ全体をコードブロックで囲んで出力する」旨を追加した。AIの回答をコピーボタンでまとめてコピーできるようになる
- コードブロックで丸ごと囲まれたパッチも、クリップボード監視で自動検知できるようにした
- 「初回用（完全版）」の指示文に、記号のエスケープ規則の説明を追加した

### 修正
- コードエディタで、本文の右端と最終行がスクロールバーに隠れることがある不具合を修正した
- AIへの指示文をコピーしただけで、パッチとして誤検知されることがある不具合を修正した

## 1.0.3

### 追加
- テーマを9種類から選べるようにした（ライト／ダークに加え、セピア・GitHub・Solarized Light／Dark・Nord・Dracula・ナイト）
- 本文と等幅の書体を、それぞれ設定から選べるようにした
- インデントガイド（字下げの位置を示す縦線）を追加した。「表示しない」「折りたたみできる範囲のみ」「すべての字下げ」から選べる
- 折りたたみを拡張した。マーカーにマウスを乗せると対応する範囲が強調され、「すべて折りたたむ」「すべて展開する」「コメントをまとめて折りたたむ」をコマンドパレットから実行できる
- コードの中の色（`#RRGGBB`・`rgb()`・`hsl()`）の手前に色見本を表示し、クリックすると色を選び直せるようにした。書き換えても元の書き方（大文字小文字・桁数・区切り方）はそのまま保たれる
- Markdownの編集を補助するようにした。リストや引用はEnterで続きが作られ、空のままEnterを押すと抜けられる。表の中ではTabとShift+Tabでセルを移動でき、最後のセルでEnterを押すと行が増える
- ツールチップの詳しさを4段階（詳しい／ふつう／最低限／表示しない）から選べるようにした

### 改善
- 折りたたみマーカーから伸びていた縦線と横線（L字の線）を表示しないようにした
- 折りたたみマーカーの左右の余白を広げ、左右対称にした
- 行番号や折りたたみの帯の上では、マウスカーソルが矢印になるようにした（従来は文字入力用の形のままだった）

### 修正
- Markdownプレビューで、コードブロックや表に横スクロールバーが出たとき、最後の行に重なって読めなくなる不具合を修正した
- 折りたたみマーカーにマウスを乗せたとき、縦線の強調がちらつく不具合を修正した。あわせて、カーソルを動かすたびに画面全体のレイアウトを計算し直していた無駄も解消した
- ファイルの削除を続けて行ったとき、ごく稀にアプリが停止することがある不具合を修正した（削除の取り消し用の記録が、複数の処理から同時に書き換えられていた）

## 1.0.2

### 改善
- ウィンドウのタイトルバーの色を、アプリのテーマ（ライト／ダーク）に合わせるようにした（Windows 11以降でのみ有効。Windows 10以下では従来どおりOS標準のタイトルバー）

## 1.0.1

### 改善
- プロジェクト選択に、マウスカーソルを乗せるだけでホイールによる切り替えができるようにした（クリックやフォーカスは不要）。先頭・末尾では止まり、ドロップダウンを開いている間は一覧のスクロールが優先される

## 1.0.0.0

最初の公開版。
